import { BasicDirective } from './basic-directive';

xdescribe('BasicDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new BasicDirective();
  //   expect(directive).toBeTruthy();
  // });
});
